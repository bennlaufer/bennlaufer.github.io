---
interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/resume', label: 'Resume' },
  { href: '/projects', label: 'Projects' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];

function isActive(href: string, path: string) {
  if (href === '/') return path === '/' || path === '';
  return path.startsWith(href);
}
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-slate-900/80 backdrop-blur-md border-b border-slate-700/30">
  <div class="max-w-5xl mx-auto px-6 lg:px-10 py-4 flex items-center justify-between">
    <!-- Left: logo -->
    <a href="/" class="flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" class="w-9 h-9 sm:w-10 sm:h-10">
        <defs>
          <linearGradient id="nav-logo-g" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#3b82f6"/>
            <stop offset="100%" stop-color="#8b5cf6"/>
          </linearGradient>
        </defs>
        <text x="4" y="26" font-family="system-ui, sans-serif" font-size="28" font-weight="bold" fill="url(#nav-logo-g)">B</text>
      </svg>
    </a>

    <!-- Desktop nav links -->
    <div class="hidden md:flex items-center gap-1">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class:list={[
            'px-3 py-2 rounded-lg text-sm font-medium transition-all duration-200',
            isActive(link.href, currentPath)
              ? 'text-accent-blue'
              : 'text-gray-400 hover:text-white hover:bg-white/5',
          ]}
        >
          {link.label}
        </a>
      ))}
    </div>

    <!-- Mobile hamburger -->
    <button
      id="mobile-menu-toggle"
      class="md:hidden p-2 -mr-2 text-gray-400 hover:text-white transition-colors"
      aria-label="Toggle menu"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>

  <!-- Mobile dropdown menu -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-slate-700/30 bg-slate-900/95 backdrop-blur-md">
    <div class="px-6 py-3 space-y-1">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class:list={[
            'block px-3 py-2.5 rounded-lg text-sm font-medium transition-all duration-200',
            isActive(link.href, currentPath)
              ? 'text-accent-blue bg-white/5'
              : 'text-gray-400 hover:text-white hover:bg-white/5',
          ]}
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>
</nav>

<script>
  function initMobileMenu() {
    const toggle = document.getElementById('mobile-menu-toggle');
    const menu = document.getElementById('mobile-menu');
    toggle?.addEventListener('click', () => {
      menu?.classList.toggle('hidden');
    });
  }
  document.addEventListener('astro:page-load', initMobileMenu);
  document.addEventListener('astro:before-preparation', () => {
    document.getElementById('mobile-menu')?.classList.add('hidden');
  });
</script>
